const initState = {
  friends: [
    { id: 1, name: "Ihor" },
    { id: 2, name: "Andriy" },
    { id: 3, name: "Aloha" },
  ],
};

const sitebarReducer = (state = initState, action) => {
  return state;
};

export default sitebarReducer;
